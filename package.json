{
  "name": "edge-duplicate-tab-remover-playwright",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "edge-duplicate-tab-remover-playwright": "file:"
  }
}
